'use server'

/**
 * Server action to handle sign-in logic with Supabase Auth.
 */
export async function loginAction(formData: FormData) {
  // To be implemented: const { email, password } = Object.fromEntries(formData)
  console.log('Login action triggered')
}
